{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "sst-env -- vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "shared": "workspace:^"
  },
  "devDependencies": {
    "@serverless-stack/static-site-env": "^1.15.0",
    "typescript": "^4.8.2",
    "vite": "^3.0.7"
  }
}
